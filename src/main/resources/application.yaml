spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/jp-codeAB/config-arka.git
          search-paths: config/

server:
  port:
    8888